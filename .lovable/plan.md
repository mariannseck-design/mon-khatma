

## Remplacer "blocs" par "portions" sur la carte Muraja'a

**Fichier** : `src/pages/AccueilPage.tsx` (ligne 477)

Remplacer `bloc${pendingReviews > 1 ? 's' : ''} à réviser` par `portion${pendingReviews > 1 ? 's' : ''} à réviser`.

